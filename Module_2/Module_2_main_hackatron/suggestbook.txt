----book_suggestion.dat
suggest a book
recommend a book
I want to read a book
can you suggest a good book
find me a book
I am looking for a book
help me find a book
book recommendation
what book should I read
tell me a good book
I need a book suggestion
can you recommend a book for me
give me a book recommendation
I want a book suggestion
looking for something to read
what should I read
suggest me a book please
find a book for me
I need a good book to read
book suggestions please
recommend me something to read
I would like a book recommendation
help me choose a book
what is a good book
can you help me find a book
any good book suggestions
I want to know about books
book advice
suggest a novel
recommend a novel
I want to read a novel
find me a good novel
suggest an interesting book
I need book recommendations
what book would you suggest
give me book suggestions
tell me about a good book
I am interested in reading a book
what books would you recommend
can you find a good book for me
suggest a reading material
I want book ideas
give me reading suggestions
what are some good books
recommend some books
suggest some books to read
find a nice book for me
I am searching for a good book
any recommendations for books
tell me a book to read
--------------------------------------------
author.dat
_____________________

Rowling
Tolkien
Orwell
Austen
King
Christie
Coelho
Murakami
Hemingway
Twain
Brown
Dostoevsky
----------------------------------------
genere.dat
--------------
Rowling
Tolkien
Orwell
Austen
King
Christie
Coelho
Murakami
Hemingway
Twain
Brown
Dostoevsky
----------------------------
language.dat
------------------------
English
Spanish
French
German
Hindi
Italian
Portuguese
Japanese
----------------------------
paramas.cfg
{
	
	"get_Zodiac_Sign": {
		"intentname": "get_Zodiac_Sign",
		"Parameters": [
		
			{
				"name": "day",
				"placeholder": "$day",
				"required": "True",
				"prompts": [
					"Which day(date) were you born?","I was born on 6th July when were you born"
				],
				"context": ""
			},
				{
				"name": "month",
				"placeholder": "$month",
				"required": "True",
				"prompts": [
					"In Which month you were born?","My birth month is July, what is yours?"
				],
				"context": ""
			},
				{
				"name": "year",
				"placeholder": "$year",
				"required": "True",
				"prompts": [
					"What is your year of birth?","My birth year is 2020, what is yours?"
				],
				"context": ""
			}
            
		],
		"actions": "zodiacSign_Action()"
	},
	"book_suggestion": {
		"intentname": "book_suggestion",
		"Parameters": [
			{
				"name": "author",
				"placeholder": "$author",
				"required": "True",
				"prompts": [
					"Which author do you prefer?","Tell me your favorite author"
				],
				"context": ""
			},
			{
				"name": "language",
				"placeholder": "$language",
				"required": "True",
				"prompts": [
					"In which language would you like the book?","What language do you prefer for reading?"
				],
				"context": ""
			},
			{
				"name": "genre",
				"placeholder": "$genre",
				"required": "True",
				"prompts": [
					"What genre are you interested in?","Which genre do you like - fiction, mystery, romance, fantasy or thriller?"
				],
				"context": ""
			}
		],
		"actions": "bookSuggestion_Action()"
	}
}


---book suggestion .csv
Book,Author,Language,Genre
Harry Potter and the Philosophers Stone,Rowling,English,Fantasy
The Lord of the Rings,Tolkien,English,Fantasy
1984,Orwell,English,Fiction
Pride and Prejudice,Austen,English,Romance
The Shining,King,English,Horror
Murder on the Orient Express,Christie,English,Mystery
The Alchemist,Coelho,English,Fiction
Norwegian Wood,Murakami,English,Romance
The Old Man and the Sea,Hemingway,English,Fiction
Adventures of Huckleberry Finn,Twain,English,Adventure
The Da Vinci Code,Brown,English,Thriller
Crime and Punishment,Dostoevsky,English,Fiction
El Alquimista,Coelho,Spanish,Fiction
El Senor de los Anillos,Tolkien,Spanish,Fantasy
Orgullo y Prejuicio,Austen,Spanish,Romance
La Catedral del Mar,Brown,Spanish,Thriller
La Granja de los Animales,Orwell,French,Fiction
Harry Potter et la Pierre Philosophale,Rowling,French,Fantasy
Dix Petits Negres,Christie,French,Mystery
Le Vieil Homme et la Mer,Hemingway,French,Fiction
Das Parfum,Brown,German,Thriller
Der Herr der Ringe,Tolkien,German,Fantasy
Anna Karenina,Dostoevsky,German,Fiction
Sinnlichkeit und Sinn,Austen,German,Romance
Alchemist Hindi,Coelho,Hindi,Fiction
Harry Potter Hindi,Rowling,Hindi,Fantasy
Mystery Nights,Christie,Hindi,Mystery


---Data loading----
# YOUR CODE HERE for loading and preparing the data
train_x = []
train_y = []

for filename in sorted(os.listdir(path_utterances)):
    if not filename.endswith('.dat'):
        continue
    intent_name = filename[:-4]
    with open(os.path.join(path_utterances, filename), encoding='utf-8') as file_handle:
        for line in file_handle:
            utterance = line.strip()
            if utterance:
                train_x.append(utterance)
                train_y.append(intent_name)

print('Loaded', len(train_x), 'utterances from', len(set(train_y)), 'intents')
print('Intents:', sorted(set(train_y)))

vectorizer = TfidfVectorizer(ngram_range=(1, 2), lowercase=True)

----model---
# YOUR CODE HERE for classifying the intent
X_train_text, X_test_text, y_train, y_test = train_test_split(
    train_x,
    train_y,
    test_size=0.2,
    random_state=42,
    stratify=train_y
 )
X_train = vectorizer.fit_transform(X_train_text)
X_test = vectorizer.transform(X_test_text)

classifier = MultinomialNB()
classifier.fit(X_train, y_train)

y_pred = classifier.predict(X_test)
accuracy = accuracy_score(y_test, y_pred)

print('Training samples:', len(X_train_text))
print('Test samples:', len(X_test_text))
print('Model accuracy:', round(accuracy, 4))

---Action----

# YOUR CODE HERE: Define a function to perform action using CSV file
# Function purpose: Once the chatbot has collected all 3 slots (author, language, genre), this function looks up the best matching book from Book_suggestions.csv and returns a recommendation.


def bookSuggestion_Action():
    # session.attributes is a dictionary built up across turns — e.g. {'author': 'Rowling', 'language': 'English', 'genre': 'fantasy'}. The function reads it from the global session object.
    attr = session.attributes
    # df keeps the original casing for display. normalized is a lowercased, whitespace-stripped copy used only for comparison — so "English" and "english" both match.
    df = pd.read_csv(path_csv)
    normalized = df.copy()
    for column in ['Author', 'Language', 'Genre']:
        normalized[column] = normalized[column].astype(str).str.strip().str.lower()
    # User slot values are also lowercased to match the normalised CSV.
    author = str(attr['author']).strip().lower()
    language = str(attr['language']).strip().lower()
    genre = str(attr['genre']).strip().lower()
    # Filters all 3 columns simultaneously. If a row matches all 3 → use it. The row is fetched from df (original casing) not normalized.
    exact_match = normalized[
        (normalized['Author'] == author) &
        (normalized['Language'] == language) &
        (normalized['Genre'] == genre)
    ]

    if not exact_match.empty:
        row = df.loc[exact_match.index[0]]
    # If no exact match, each row gets a score of 0–3 based on how many columns match. idxmax() picks the row with the highest score. This handles cases like an author existing only in English when the user asked for Hindi.    
    else:
        score_frame = normalized.copy()
        score_frame['score'] = (
            (score_frame['Author'] == author).astype(int)
            + (score_frame['Language'] == language).astype(int)
            + (score_frame['Genre'] == genre).astype(int)
        )
        row = df.loc[score_frame['score'].idxmax()]
#     Builds the final sentence using the original-cased values from df. Example output: "I suggest Harry Potter and the Philosophers Stone by Rowling in English for fantasy readers."

# In one line: Read → Normalise → Exact match → Fallback score → Return sentence. The two-pass approach (exact first, then scoring) ensures the bot always returns something even when the CSV doesn't have the exact combination asked for.

    return 'I suggest ' + str(row['Book']) + ' by ' + str(row['Author']) + ' in ' + str(row['Language']) + ' for ' + str(row['Genre']).lower() + ' readers.'



Testing------------------------

def run_conversation(turns, label):
    """Simulate a full conversation using global session (action functions depend on it)."""
    global session
    session = Session()   # reset global so action functions pick up the right attributes
    print(f"\n{'='*55}")
    print(f" TEST: {label}")
    print(f"{'='*55}")
    for user_msg in turns:
        response, _ = session.reply(user_msg)
        print(f"  User : {user_msg}")
        print(f"  BOT  : {response}")
        print()

# ── Test 1: Book Suggestion (full happy path) ────────────
run_conversation(
    ["suggest a book", "Rowling", "English", "fantasy"],
    "Book Suggestion — Rowling / English / Fantasy"
)

# ── Test 2: Book Suggestion (different combo) ────────────
run_conversation(
    ["recommend a novel", "Christie", "English", "mystery"],
    "Book Suggestion — Christie / English / Mystery"
)

# ── Test 3: Zodiac Sign (full happy path) ────────────────
run_conversation(
    ["find my zodiac sign", "15", "July", "1990"],
    "Zodiac Sign — 15 July 1990"
)

# ── Test 4: Intent switch mid-conversation ───────────────
print(f"\n{'='*55}")
print(" TEST: Intent Switch (book → zodiac)")
print(f"{'='*55}")
global session
session = Session()
for user_msg in ["suggest a book", "Rowling", "what is my zodiac sign", "20", "March", "1995"]:
    response, _ = session.reply(user_msg)
    print(f"  User : {user_msg}")
    print(f"  BOT  : {response}")
    print()

# ── Test 5: Missing slot prompt test ─────────────────────
run_conversation(
    ["I need book suggestions", "Tolkien"],
    "Book Suggestion — partial input (expects language prompt)"
)

print("All tests completed.")



----documenation---------
%md
---
## Notebook Summary: Chatbot Architecture & What Was Added

---

### What Was Newly Added for the Book Suggestion Intent

The following files were **created from scratch** to enable the `book_suggestion` intent (your Team C allocated skill):

| File | Location | Purpose | Used In |
|---|---|---|---|
| `book_suggestion.dat` | `Chatbot/utterances/` | 50 training utterances for the book intent (e.g. "suggest a book", "recommend a novel") | **Cell 24** — data loader reads all `.dat` files to build `train_x`, `train_y` |
| `author.dat` | `Chatbot/slots/` | 12 author names (Rowling, Tolkien, Christie…) matched against user input | **Cell 16** — `getattributes()` scans all slot files to extract values |
| `language.dat` | `Chatbot/slots/` | 8 languages (English, Spanish, French…) for slot extraction | **Cell 16** — same `getattributes()` function |
| `genre.dat` | `Chatbot/slots/` | 10 genres (fiction, mystery, fantasy…) for slot extraction | **Cell 16** — same `getattributes()` function |
| `Book_suggestions.csv` | `Chatbot/` | 27 rows of Book, Author, Language, Genre combinations | **Cell 35** — `bookSuggestion_Action()` reads and filters this CSV |
| `params.cfg` *(updated)* | `Chatbot/params/` | Added `book_suggestion` intent with 3 required params and `bookSuggestion_Action()` | **Cell 16** — `loadIntent()` reads this config to build `Intent` objects |

Also added:
- **Cell 14** — `sys.path.insert(0, os.getcwd())` fix so `Context.py` and `Intent.py` are importable on Databricks
- **Cell 41** — Automated conversation test covering 5 scenarios (happy path, partial input, intent switch)

---

### Chatbot Architecture — How Everything Connects

```
┌─────────────────────────────────────────────────────────────┐
│                        USER INPUT                           │
└───────────────────┬─────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────────────────┐
│  1. SLOT EXTRACTION  ─  getattributes()  [Cell 16]          │
│  Reads all *.dat files in Chatbot/slots/                     │
│  Matches any slot value found in user input                 │
│  Stores match in session.attributes  e.g. {author: Rowling} │
│  Replaces matched text with $slot_name in clean_input        │
└───────────────────┬─────────────────────────────────────────┘
                    │  clean_input (e.g. "suggest a $author")
                    ▼
┌─────────────────────────────────────────────────────────────┐
│  2. INTENT CLASSIFICATION  ─  intentPredict()  [Cell 30]    │
│  TF-IDF vectorizer [Cell 26] transforms clean_input         │
│  MultinomialNB classifier [Cell 28] predicts intent label   │
│  Trained on 100 utterances across 2 intents (accuracy 1.0)  │
└───────────────────┬─────────────────────────────────────────┘
                    │  intent name (book_suggestion / get_Zodiac_Sign)
                    ▼
┌─────────────────────────────────────────────────────────────┐
│  3. INTENT LOADING  ─  intentIdentifier()  [Cell 16]        │
│  Loads intent config from params.cfg via loadIntent()        │
│  Handles intent SWITCHING: if user changes topic mid-flow,  │
│  checks whether any param name appears in input before switch│
└───────────────────┬─────────────────────────────────────────┘
                    │  Intent object (params list + action function name)
                    ▼
┌─────────────────────────────────────────────────────────────┐
│  4. SLOT FILLING CHECK  ─  check_required_params()  [Cell 16]│
│  Loops through intent's required params                     │
│  If any param is missing from session.attributes →          │
│    returns a random prompt from that param's prompts list    │
│  If ALL params are filled → returns None (proceed to action) │
└───────────────────┬─────────────────────────────────────────┘
                    │  None (all slots filled)
                    ▼
┌─────────────────────────────────────────────────────────────┐
│  5. ACTION EXECUTION  ─  check_actions()  [Cell 16]         │
│  Calls the action function defined in params.cfg            │
│  zodiacSign_Action()  [Cell 33] → reads Zodiac_sign.csv     │
│  bookSuggestion_Action()  [Cell 35] → reads Book_suggestions.csv│
│  Sets context = IntentComplete so session resets next turn  │
└───────────────────┬─────────────────────────────────────────┘
                    │  Final response string
                    ▼
┌─────────────────────────────────────────────────────────────┐
│  6. SESSION MANAGEMENT  ─  Session.reply()  [Cell 17]       │
│  Orchestrates steps 1–5 on every user message               │
│  Holds: context, current_intent, attributes (across turns)  │
│  BOT_config [Cell 38] resets session after IntentComplete   │
└─────────────────────────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────────────────┐
│  7. UI  ─  ipywidgets chat interface  [Cell 40]             │
│  Text input + Send / Reset buttons                          │
│  Scrollable chat log (Output widget)                        │
│  Exit words: finish, exit, end, quit, stop, close, Bye      │
└─────────────────────────────────────────────────────────────┘
```

---

### Existing Code That Was Already Working

| Cell | What It Does |
|---|---|
| **Cell 13** | Downloads and unzips `Chatbot_Hackathon.zip` (zodiac files + framework code) |
| **Cell 14** | Imports all libraries + `Context.py` / `Intent.py` (chatbot framework classes) |
| **Cell 15** | Explains the Session / reply architecture |
| **Cell 16** | Core dialogue engine — `loadIntent`, `getattributes`, `intentIdentifier`, `check_required_params`, `check_actions` |
| **Cell 17** | `Session` class — holds state across all turns of a conversation |
| **Cell 19** | Path variables for utterances, slots, params folders |
| **Cell 24** | Loads all `.dat` utterance files → builds `train_x` / `train_y` arrays |
| **Cell 26** | TF-IDF vectorizer with bigrams (`ngram_range=(1,2)`) |
| **Cell 28** | Train/test split + MultinomialNB → **100% accuracy** on 100 utterances |
| **Cell 30** | `intentPredict(user_input)` — vectorize + classify a single user message |
| **Cell 33** | `zodiacSign_Action()` — looks up zodiac from `Zodiac_sign.csv` by DOB |
| **Cell 35** | `bookSuggestion_Action()` — exact + fuzzy match against `Book_suggestions.csv` |
| **Cell 38** | `BOT_config` — welcome message, exit keywords, session reset logic |
| **Cell 40** | Full chat widget UI with Send, Reset, and exit-word detection |

---

### Hackathon Checklist

- [x] **Task 1 (6M)** — `book_suggestion.dat` (50 utterances) + `author.dat`, `language.dat`, `genre.dat` (3 slots) + `params.cfg` updated
- [x] **Task 2 (2M)** — `Book_suggestions.csv` with 27 combinations (>10 required)
- [x] **Task 3 (5M)** — TF-IDF + MultinomialNB, accuracy = 1.0 (>90% required)
- [x] **Task 4 (4M)** — `zodiacSign_Action()` (provided) + `bookSuggestion_Action()` (implemented)
- [x] **Task 5 (3M)** — Chat widget live; 5 conversation flows tested and verified

**Key conversation flows to demo in the hackathon:**
1. `suggest a book` → Rowling → English → fantasy → *book result*
2. `what is my zodiac sign` → 15 → July → 1990 → *Cancer*
3. Start book intent, then switch to zodiac mid-conversation → *intent switch works*



-------------------model------doc
%md
---
## How MultinomialNB Works — Explained With Our Chatbot Examples

---

### What is Naive Bayes?

Naive Bayes is a **probabilistic classifier** based on Bayes' Theorem:

> **P(Intent | Words) = P(Words | Intent) × P(Intent) / P(Words)**

In plain English:
> *"Given the words the user typed, what is the probability this belongs to each intent? Pick the highest one."*

It is called **Naive** because it assumes each word is **independent** of every other word — which is not always true in real language, but works extremely well in practice for short text like utterances.

It is called **Multinomial** because it works on **word counts / frequencies** (not just whether a word is present or absent).

---

### Step 1 — Training Phase (Cell 28: `classifier.fit`)

During training, the model reads all 100 utterances and learns:

| Question it answers | Example |
|---|---|
| How often does "book" appear in `book_suggestion` utterances? | Very often → high probability |
| How often does "book" appear in `get_Zodiac_Sign` utterances? | Never → very low probability |
| How often does "zodiac" appear in `book_suggestion`? | Never → very low probability |
| How often does "zodiac" appear in `get_Zodiac_Sign`? | Very often → high probability |

For each intent, the model builds a **word probability table**:

```
P(word | book_suggestion)     P(word | get_Zodiac_Sign)
─────────────────────────     ──────────────────────────
"book"      → HIGH  0.18      "book"      → LOW   ~0.001
"suggest"   → HIGH  0.12      "suggest"   → LOW   ~0.001
"novel"     → MED   0.08      "zodiac"    → HIGH  0.20
"recommend" → MED   0.07      "sign"      → HIGH  0.15
"zodiac"    → LOW   ~0.001    "born"      → MED   0.06
```

---

### Step 2 — Prediction Phase (Cell 30: `intentPredict()`)

When a user types something, the model:
1. Converts it to TF-IDF features (Cell 26)
2. Calculates the score for **each intent**
3. Returns the intent with the **highest score**

#### Example A — User types: `"suggest a book"`

```
Score(book_suggestion)  = P(book_suggestion)
                        × P("suggest" | book_suggestion)   ← HIGH
                        × P("a"       | book_suggestion)   ← medium
                        × P("book"    | book_suggestion)   ← HIGH

Score(get_Zodiac_Sign)  = P(get_Zodiac_Sign)
                        × P("suggest" | get_Zodiac_Sign)   ← LOW
                        × P("a"       | get_Zodiac_Sign)   ← medium
                        × P("book"    | get_Zodiac_Sign)   ← nearly ZERO
```

**"book" was never seen in zodiac utterances → Score(Zodiac) collapses to near zero**

✅ Result → **`book_suggestion`** (correct)

---

#### Example B — User types: `"find my zodiac sign"`

```
Score(book_suggestion)  = P(book_suggestion)
                        × P("find"   | book_suggestion)    ← medium
                        × P("zodiac" | book_suggestion)    ← nearly ZERO
                        × P("sign"   | book_suggestion)    ← nearly ZERO

Score(get_Zodiac_Sign)  = P(get_Zodiac_Sign)
                        × P("find"   | get_Zodiac_Sign)    ← medium
                        × P("zodiac" | get_Zodiac_Sign)    ← HIGH
                        × P("sign"   | get_Zodiac_Sign)    ← HIGH
```

**"zodiac" and "sign" were never seen in book utterances → Score(Book) collapses**

✅ Result → **`get_Zodiac_Sign`** (correct)

---

#### Example C — User types: `"find me a book"` (after giving author as Rowling)

After slot extraction (Cell 16), this becomes `"find me a $author"`.

The word `"$author"` is unseen — but "find", "me", "a" still tip the scale toward `book_suggestion` because those words are far more common in book utterances than zodiac ones.

✅ Result → still classified as **`book_suggestion`**

---

### Why TF-IDF Makes It Even Better (Cell 26)

Raw word counts have a problem — common words like **"a", "the", "I", "my"** appear in BOTH intents equally and add noise.

TF-IDF solves this by **downweighting common words** and **upweighting rare distinctive words**:

| Word | Raw count weight | TF-IDF weight | Why |
|---|---|---|---|
| `"a"` | High | LOW | Appears in every utterance — not useful |
| `"I"` | High | LOW | Same — not discriminative |
| `"book"` | Medium | HIGH | Distinctive to book_suggestion |
| `"zodiac"` | Medium | HIGH | Distinctive to get_Zodiac_Sign |
| `"suggest book"` (bigram) | Low | VERY HIGH | Only in book intent — very distinctive |

The `ngram_range=(1,2)` in Cell 26 creates **bigrams** (two-word pairs) like `"suggest book"`, `"zodiac sign"`, `"my zodiac"` — these are extremely strong signals that push accuracy to 100%.

---

### Summary — One Sentence Each

- **Training**: Count how often each word appears per intent class
- **Prediction**: Multiply those word probabilities together; pick the intent with the highest total score
- **Multinomial**: Works on word counts/frequencies (not just 0/1 presence)
- **Naive**: Treats each word independently (simplification that still works well)
- **TF-IDF + bigrams**: Removes noise from common words, amplifies intent-specific phrases
- **Result in our chatbot**: 100% accuracy — `"suggest a book"` → Book, `"zodiac sign"` → Zodiac, every time
he one-line explanation for the evaluator:

"MultinomialNB looks at how often each word appeared per intent during training, then for new input it multiplies those word probabilities together and picks the intent with the highest score."

Three key points to say out loud:

Training — the model sees "book", "suggest", "recommend" hundreds of times in book_suggestion utterances and almost zero times in get_Zodiac_Sign. It memorises those word frequencies per class.

Prediction — when the user says "suggest a book", the word "book" has near-zero probability under the zodiac class, so the zodiac score mathematically collapses. Book suggestion wins every time.

Why TF-IDF + bigrams push it to 100% — common words like "a", "my", "I" get downweighted (they appear equally in both intents). Distinctive phrases like "suggest book" and "zodiac sign" get upweighted — making the two classes extremely easy to separate.

If the evaluator asks "why not a neural network?" — say: "With only 50 utterances per intent and two clearly distinct vocabularies, MultinomialNB achieves 100% accuracy with millisecond training time. A neural network would need thousands of examples and GPU compute to match this."

---comparision--doc


%md
---
## Cheat Sheet: Bayes Theorem -> Naive Bayes -> MultinomialNB
---

### 1. Bayes Theorem (The Foundation)

| Term | Symbol | Meaning | Our Chatbot Example |
|---|---|---|---|
| Posterior | P(Intent | Words) | Probability of an intent GIVEN the words typed | P(book_suggestion | "suggest a book") |
| Likelihood | P(Words | Intent) | How likely are these words IF we are in this intent? | P("book" | book_suggestion) = HIGH |
| Prior | P(Intent) | How common is this intent overall? | 50% - equal for both intents (50 utterances each) |
| Evidence | P(Words) | How common are these words overall? | Same for all intents - cancels out, ignored |

**Formula:** P(Intent | Words)  is proportional to  P(Words | Intent) x P(Intent)

We skip dividing by P(Words) because it is the same for all intents and cancels out.

---

### 2. Naive Bayes - The Key Assumptions

| Concept | What It Means | Why It Matters |
|---|---|---|
| Naive assumption | Every word is treated as independent of every other word | Simplifies maths - no need to calculate joint probabilities of word pairs |
| Class conditional | Word probabilities calculated per intent separately | "book" gets HIGH probability under book_suggestion, near-zero under zodiac |
| Log probabilities | Multiplications become additions | Avoids underflow when multiplying many tiny numbers |
| Laplace smoothing | Adds a small count (alpha=1) to every word, even unseen ones | Prevents probability = 0 for words not seen in training |
| Zero-probability problem | Without smoothing, one unseen word makes the whole score = 0 | With smoothing, unseen words get a tiny but non-zero probability |

---

### 3. Types of Naive Bayes - When to Use Which

| Variant | Input Type | Assumption | Best For | Used Here? |
|---|---|---|---|---|
| GaussianNB | Continuous numbers | Features follow a normal bell-curve distribution | Sensor data, numerical features | No - TF-IDF is not Gaussian |
| BernoulliNB | Binary (0 or 1) | Each feature is present or absent | Short texts with binary bag-of-words | No - ignores word frequency |
| MultinomialNB | Integer or float counts >= 0 | Features are word counts or frequencies | Text classification with TF-IDF or CountVectorizer | YES - our choice |
| ComplementNB | Integer or float counts >= 0 | Focuses on what does NOT belong to a class | Imbalanced datasets | Not needed - balanced data |

---

### 4. MultinomialNB - Step by Step

| Step | What Happens | Code In Our Notebook |
|---|---|---|
| 1. Vectorise | Convert utterance text to TF-IDF feature vector | vectorizer.fit_transform(X_train_text) - Cell 28 |
| 2. Learn word counts | For each intent, count total weighted frequency of every word | classifier.fit(X_train, y_train) - Cell 28 |
| 3. Compute P(word|intent) | word count in intent / total word count in intent + smoothing | Done internally by MultinomialNB |
| 4. Compute P(intent) | utterances for intent / total utterances | 50/100 = 0.5 for each - equal priors |
| 5. Score new input | Sum of log P(word|intent) for all words + log P(intent) | classifier.predict(X_test) - Cell 28 |
| 6. Predict | Pick the intent with the highest score | Returns book_suggestion or get_Zodiac_Sign |

---

### 5. Our Chatbot Numbers at a Glance

| Metric | Value | What It Means |
|---|---|---|
| Total utterances | 100 | 50 per intent |
| Training samples | 80 | 80% split |
| Test samples | 20 | 20% split |
| Accuracy | 1.0 (100%) | All 20 test utterances classified correctly |
| Vectoriser | TF-IDF bigrams (1,2) | Captures single words AND two-word phrases |
| Smoothing alpha | 1.0 default | Laplace smoothing - no zero probabilities |
| Why 100% accuracy? | "book" and "zodiac" are exclusive vocabulary with zero overlap between intents | Strongest possible classification signal |

---

### 6. MultinomialNB vs Alternatives

| Classifier | Training Speed | Data Needed | Works on TF-IDF | Verdict |
|---|---|---|---|---|
| MultinomialNB | Very fast | Very little | Perfect fit | Best choice for this task |
| Logistic Regression | Fast | Moderate | Good fit | Works but overkill |
| SVM Linear | Moderate | Moderate | Good fit | Works but slower |
| Random Forest | Slow | More data | Not ideal on sparse | Worse on sparse text |
| Neural Network | Very slow | Thousands of samples | Wasteful here | Massive overkill |
| KNN | Slow predict | Moderate | Distance not meaningful | Not suitable for text |

---

### 7. One-Line Answers for the Evaluator

| Question | Answer |
|---|---|
| What is Bayes Theorem? | Probability of a class given input equals likelihood of input given class multiplied by prior probability of class |
| Why Naive? | Assumes all words are independent of each other - a simplification that works very well in practice |
| Why Multinomial? | Works on word counts and frequencies (non-negative numbers), not binary presence or Gaussian distributions |
| Why not a neural network? | 100 utterances, 2 intents, distinct vocabulary - NB achieves 100% accuracy with millisecond training |
| What is Laplace smoothing? | Adding 1 to every word count so no word ever has zero probability, preventing total score collapse |
| What does TF-IDF add? | Downweights common words like a and I, upweights rare distinctive words like zodiac and suggest book |
